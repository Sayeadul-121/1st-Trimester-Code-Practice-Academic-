#include <stdio.h>
int main()
{
    /*

        Program that will define a constant using “CONST” and print the value.

    */

    const float pi = 3.14;
    const float ratio = 1.62;

    printf("The value of pi: %.2f\n", pi);
    printf("The value of golden ratio: %.2f", ratio);
    return 0;
}